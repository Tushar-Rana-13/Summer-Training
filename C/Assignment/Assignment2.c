 // Assignment No. - 2 

 # include<stdio.h> 
 # include<math.h>
 int main() {

// 1. C Program to find compound interest

    // printf("Enter the principal amount, rate of interest and time in years: ") ;
    // float p , r, t ; 
    // scanf("%f%f%f" , &p , &r , &t) ;
    // float ci = p * (1 + r/100)*t-p ;
    // printf("Compound Interest = %.2f" , ci) ;

// 2 . C Program to find total average and percentage of five subjects 
        // int a , b , c , d , e ;
        // printf("Enter the marks of five subjects : ") ;
        // scanf("%d%d%d%d%d" , &a , &b , &c , &d , &e) ;
        // int average = (a + b + c + d + e) / 5 ;
        // printf("Average = %d\n" , average) ;
        // float percentage = average ;  // average is already a percentage (out of 100)
        // printf("Percentage = %.2f" , percentage) ;


// 3 .  C Program to find area of equilateral triangle .
        // int side ;
        // printf("Enter the side of equilateral triangle :") ;
        // scanf("%d" , &side) ;
        // float area = (1.732 /4) *(side * side) ;
        // printf("Area of equilateral triangle = %.2f" , area) ;

//4 .  C program to print characters in a reverse way . 
        // char str[100] ;
        // printf("Enter 3 characters :") ;
        // scanf("%s" , str) ;
        // printf("Characters in reverse order : ") ;
        // for (int i = 2 ; i >= 0 ; i--) {
        //     printf("%c" , str[i]) ;
        // }

// 5 .  C program that accepts two item's weight and number of purchase and calculate the average value of the items .
        // float w1 , w2 ;
        // int n1 , n2 ;
        // printf("Enter the weight and number of purchases ") ;
        // scanf("%f%d%f%d" , &w1 , & n1 , &w2 , &n2) ;
        // float avg = (w1 * n1 + w2 * n2) / (n1 + n2) ;
        // printf("Average = %.2f" , avg) ;

// 6.   C Program to calculate bike's average consumption from the given total distance traveled and spent fuel .
        // int distance ;
        // float fuel ;
        // printf("Enter distance and fuel :") ;
        // scanf("%d%f" , &distance , &fuel) ;
        // int average = distance/fuel  ;
        // printf("Average :%d" , average) ;

// 7.  C Program to find distance between two points .
        // int x1 , y1 , x2 , y2 ;
        // printf("Enter the coordinates of two points (x1 y1 x2 y2) :") ;
        // scanf("%d%d%d%d" , &x1 , &y1 , &x2 , &y2) ;
        // float distance = sqrt((x2 - x1)*(x2 - x1) + (y2 - y1)*(y2 - y1)) ;
        // printf("Distance between two points = %.2f" , distance) ;

// 8.  C Program to swap to two numbers without using a temporary variable .
        // int a , b ;
        // printf("Enter two numbers :") ;
        // scanf("%d%d" , &a , &b) ;
        // a = a + b ;
        // b = a - b ;
        // a = a - b ;
        // printf("After swapping : a = %d , b = %d" , a , b) ;

// 9 . c Program to convert Fahrenhite to Celcius 
            // float f ;
            // printf("Enter temp. in F :") ;
            // scanf("%f" , &f) ;
            // float c = (f-32) * 5/9 ;
            // printf("Temp. in C : %.2f" , c) ;

// 10 .  C Program to input principle , time and rate from user and find SI. 
            // float p , r , t ;
            // printf("Enter principal , rate and time :") ;
            // scanf("%f%f%f" , &p , &r , &t) ;
            // float si = (p * r * t) / 100 ;
            // printf("Simple Interest = %.2f" , si) ;
            
        return 0 ;
 }