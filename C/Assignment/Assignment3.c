// Assignment No. - 3 

#include <stdio.h>

int main() {
// 1 . Find maximum between two numbers using conditional operator .
    // int a , b ;
    // printf("Enter two numbers : ") ;
    // scanf("%d%d" , &a , &b) ;
    // int max = (a > b) ? a : b ;
    // printf("Maximum between %d and %d is : %d\n" , a , b , max) ;

// 2 . Max between three numbers using conditional operator .
    //    int a , b , c ;
    //    printf("Enter three numbers : ") ;
    //    scanf("%d%d%d" , &a , &b , &c) ;
    //    int max = (a > b) ? ((a > c) ? a : c) : ((b > c) ? b : c) ;
    //    printf("Maximum between %d, %d and %d is : %d\n" , a , b , c , max) ;
       
// 3 .  Number is even or odd using conditional operator .
        // int num ;
        // printf("enter a number : ") ;
        // scanf("%d" , &num) ;
        // if(num % 2 == 0) {
        //     printf("%d is an even number\n" , num) ;
        // } else {
        //     printf("%d is an odd number\n" , num) ;
        // }

// 4 . Year is leap year or not using conditional operator .
        // int year ;
        // printf("Enter a year : ") ;
        // scanf("%d" , &year) ;
        // if((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
        //     printf("%d is a leap year\n" , year) ;
        // } else {
        //     printf("%d is not a leap year\n" , year) ;
        // }

// 5 . Character is alphabet or not using conditional operator .
        // char ch ;
        // printf("Enter a character : ") ;
        // scanf("%c" , &ch) ;
        // if((ch >= 'A' && ch <= 'Z') || (ch >= 'a' && ch <= 'z')) {
        //     printf("%c is an alphabet\n" , ch) ;
        // } else {
        //     printf("%c is not an alphabet\n" , ch) ;
        // }

// 6 . Swap two numbers using bitwise operator .
        // int a , b ;
        // printf("Enter two numbers : ") ;
        // scanf("%d%d" , &a , &b) ;
        // printf("Before swapping : a = %d, b = %d\n" , a , b) ;
        // a = a ^ b ;
        // b = a ^ b ;
        // a = a ^ b ;
        // printf("After swapping : a = %d, b = %d\n" , a , b) ;

// 7 . enter a 4-digit number and print all digit .
        // int num ;
        // printf("Enter a 4-digit number : ") ;
        // scanf("%d" , &num) ;
        // printf("Digits in the number are : ") ;
        // while(num > 0) {
        //     int digit = num % 10 ;
        //     printf("%d " , digit) ;
        //     num /= 10 ;
        // }
        // printf("\n") ;

// 8 . check Whether number is positive , negative or zero using conditional operator .
        // int num ;
        // printf("Enter a number : ") ;
        // scanf("%d" , &num) ;
        // if(num > 0) {
        //     printf("%d is a positive number\n" , num) ;
        // } else if(num < 0) {
        //     printf("%d is a negative number\n" , num) ;
        // } else {
        //     printf("The number is zero\n") ;
        // }

// 9 . Write a C program to find the absolute value of a number using conditional operator.
        // int num ;
        // printf("Enter a number : ") ;
        // scanf("%d" , &num) ;
        // int absValue = (num < 0) ? -num : num ;
        // printf("Absolute value of %d is : %d\n" , num , absValue) ;

        
// 10 . Write a C program to demonstrate the use of logical operators by checking whether a given
//     number lies within a specified range (e.g., between 10 and 20).
        // int num ;
        // printf("Enter a number : ") ;
        // scanf("%d" , &num) ;
        // if(num > 10 && num < 20) {
        //     printf("%d lies within the range of 10 and 20\n" , num) ;
        // } else {
        //     printf("%d does not lie within the range of 10 and 20\n" , num) ;
        // }


    return 0 ;
}